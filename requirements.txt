# VEX IQ Simulator Dependencies
# ==============================

# Tools directory dependencies (LDraw rendering, model conversion)
pygame>=2.5.0
Pillow>=10.0.0
trimesh>=4.0.0
pygltflib>=1.16.0

# Note: simulator/ only needs Python standard library
# The C++ client bundles its own Python for the robot harness
